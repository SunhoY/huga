import React from 'react';
import {Form} from 'react-bootstrap';
import 'react-datetime';

export const HugaRegistrationView = (props) =>
    <Form horizontal onSubmit={props.onRegisterSubmit}>
    </Form>

